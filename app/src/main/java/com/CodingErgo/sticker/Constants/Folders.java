package com.CodingErgo.sticker.Constants;

import android.os.Environment;

public class Folders {
    public static final String ContentFolder =  Environment.getExternalStorageDirectory() + "/Sticker's wale/.packs/sticker/";
    public static final String ContentFolder2 =  Environment.getExternalStorageDirectory() + "/sticker's wala/.packs/sticker/";
}
